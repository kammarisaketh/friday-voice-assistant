# 🎙️ FRIDAY Assistant

Real-time AI assistant with a FastAPI backend, intent-based command handling, and a browser UI.

## Tech Stack
- Python, FastAPI
- WebSockets
- JavaScript, HTML, CSS

## Features
- Intent-based commands (info, web search, job search, etc.)
- FastAPI backend in `friday/` with `core` and `intents` modules
- Browser UI in `ui/` using mic button, logs and responses

## Run Locally

```bash
pip install -r requirements.txt
python main.py
